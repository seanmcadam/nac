#!/usr/bin/perl

package NAC::Worker::LocalDB;

use FindBin;
use lib "$FindBin::Bin/../..";
use NAC::Worker;
use NAC::Worker::Function::GetLocalSQL;
use strict;

my %functions = ();

sub new {
my ($class, $parms) = @_;

}




1;

